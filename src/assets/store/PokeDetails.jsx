/*import { createSlice } from '@reduxjs/toolkit';

// NOTA: Se eliminan todas las importaciones de tipos (type PokemonDetail, PokemonFavoritos).

// El estado inicial se define como un array vacío.
const initialState = [];

export const PokemonDetailSlice = createSlice({
  name: 'users',
  initialState, // []. Representa el array que contendrá los datos.
  
  // Los reducers están vacíos, listos para que añadas lógica.
  reducers: {},
});

// Exporta solo el reducer, ya que no hay acciones definidas aún.
export default PokemonDetailSlice.reducer;*/